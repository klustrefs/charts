suite: Namespace
templates:
  - templates/namespace.yaml
tests:
  - it: creates namespace with labels by default
    asserts:
      - equal:
          path: metadata.name
          value: klustre-system
      - equal:
          path: metadata.labels["pod-security.kubernetes.io/enforce"]
          value: privileged

  - it: skips namespace when disabled
    set:
      namespace.create: false
    asserts:
      - hasDocuments:
          count: 0
